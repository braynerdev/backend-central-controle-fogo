# ==============================================
# Environment Variables Example for Render
# ==============================================

# Application Configuration
APP_NAME=backend-central-controle-fogo

# Server Port (Render sets this automatically, usually 10000)
PORT=8088

# Database Configuration
# On Render, you can use the Internal Database URL from your PostgreSQL instance
DATABASE_URL=jdbc:postgresql://your-db-host:5432/your-database-name
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password

# JPA Configuration
JPA_DDL_AUTO=update
JPA_SHOW_SQL=false

# JWT RSA Keys (Base64 encoded)
# To generate these values, run:
#   For PUBLIC KEY:  cat src/main/resources/app.pub | base64 -w 0
#   For PRIVATE KEY: cat src/main/resources/app.key | base64 -w 0
# 
# On Windows (PowerShell):
#   For PUBLIC KEY:  [Convert]::ToBase64String([IO.File]::ReadAllBytes("src/main/resources/app.pub"))
#   For PRIVATE KEY: [Convert]::ToBase64String([IO.File]::ReadAllBytes("src/main/resources/app.key"))
JWT_PUBLIC_KEY_BASE64=your-base64-encoded-public-key-here
JWT_PRIVATE_KEY_BASE64=your-base64-encoded-private-key-here
